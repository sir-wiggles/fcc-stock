const values = {
    ADD_STOCK_REQUEST    : null,
    ADD_STOCK_SUCCESS    : null,
    ADD_STOCK_FAIL       : null,

    REMOVE_STOCK_REQUEST : null,
    REMOVE_STOCK_SUCCESS : null,
    REMOVE_STOCK_FAIL    : null,
}

for (let key in values) {
    if (true) {
        values[key] = key
    }
}

export default values;

